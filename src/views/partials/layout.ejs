<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= titulo %> · Papelería CeCy</title>
  <link rel="stylesheet" href="<%= base %>/css/styles.css">
</head>
<body>
  <header class="barra">
  <h1>Papelería · Libros</h1>
  <% if (usuario) { %>
    <div class="usuario-barra">
      <span><%= usuario.nombre %></span>
      <form action="<%= base %>/logout" method="POST" style="display:inline;">
        <button type="submit" class="btn-salir">Salir</button>
      </form>
    </div>
  <% } %>
</header>
<% if (usuario) { %>
  <nav class="menu">
    <a href="<%= base %>/">Inicio</a>
    <a href="<%= base %>/libros">Libros</a>
    <a href="<%= base %>/maestros">Maestros</a>
    <a href="<%= base %>/ventas">Ventas</a>
    <a href="<%= base %>/reportes">Reportes</a>
  </nav>
<% } %>
  <main class="contenido">
    <%- body %>
  </main>
</body>
</html>
